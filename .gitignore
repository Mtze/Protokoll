# Xcode / build artifacts
*.xcodeproj/
*.xcworkspace/
!default.xcworkspace
DerivedData/
build/
.build/
*.o
*.swiftmodule

# `make dist` output (local tester handoff, never committed)
/Protokoll.zip

# XcodeGen output (project is generated, not committed)
# (kept explicit so the generated .xcodeproj never lands in git)

# macOS
.DS_Store

# SwiftPM
Package.resolved

# fastlane
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output/

# User Xcode settings
xcuserdata/
*.xcuserstate

# Scratch / local
*.log
.env

# Local git worktrees
.worktrees/
